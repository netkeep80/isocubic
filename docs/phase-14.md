# Фаза 14: `@netkeep80/metamode` — выделение пакета в GitHub-репозиторий

Данный документ описывает задачи по выделению пакета `@netkeep80/metamode` в отдельный GitHub-репозиторий и его повторному подключению обратно в `isocubic` как внешней зависимости через GitHub Packages.

**Статус**: ✅ Завершена (TASK 88, 89, 90, 91, 92 завершены)

---

## Обзор

**Цель**: Выделить пакет `@netkeep80/metamode` из `https://github.com/netkeep80/isocubic/tree/main/packages/metamode` в отдельный GitHub-репозиторий `https://github.com/netkeep80/metamode` и подключить его обратно в `https://github.com/netkeep80/isocubic` как внешнюю зависимость через GitHub Packages.

**Публикация**: Упрощённая схема — не в реестр npmjs.com, а через GitHub Packages (`https://npm.pkg.github.com`). Установка пакета производится с указанием реестра GitHub.

**Проблема**: Пакет `packages/metamode` создан и имеет корректный `package.json`, `README.md` и исходный код, но:
- Назван как `@isocubic/metamode` (требует переименования в `@netkeep80/metamode`)
- CI/CD настроен на публикацию в npmjs.com (требует переориентации на GitHub Packages)
- Нет пошаговой инструкции по ручному выделению в отдельный репозиторий

**Решение**: Поэтапная подготовка пакета:
1. **TASK 88**: Vite Library Mode конфигурация для сборки пакета
2. **TASK 89**: CI/CD workflow для публикации в GitHub Packages (не npmjs)
3. **TASK 90**: Полное тестирование пакета с `@vue/test-utils`
4. **TASK 91**: Интеграционные тесты — использование пакета из реального приложения
5. **TASK 92**: Подробная инструкция по ручному выделению пакета в GitHub-репозиторий

---

## Планируемые TASK

### TASK 88: Vite Library Mode конфигурация ✅

**Заголовок**: `@netkeep80/metamode Phase 1 — Vite Library Mode build configuration`

**Приоритет**: Критический

**Статус**: ✅ Завершена

**Описание**:
Создать `vite.config.ts` для пакета `packages/metamode` в режиме Vite Library Mode, чтобы пакет можно было собрать в `dist/` и опубликовать через GitHub Packages. Обновить `package.json` пакета с `build` скриптом и указателями на `dist/`.

**Задачи**:

- [x] Создать `packages/metamode/vite.config.ts` с Library Mode конфигурацией
- [x] Обновить `packages/metamode/package.json`: добавить `build` скрипт, обновить `main`/`module`/`types` на `dist/`
- [x] Добавить `packages/metamode` в root `package.json` как workspace скрипт
- [x] Проверить, что пакет собирается без ошибок
- [x] Обновить `.gitignore` для игнорирования `packages/metamode/dist/`

**Созданные файлы**:

- `packages/metamode/vite.config.ts` — конфигурация Vite Library Mode для сборки пакета

**Изменённые файлы**:

- `packages/metamode/package.json` — добавлен `build` скрипт и обновлены exports на `dist/`
- `.gitignore` — добавлен `packages/metamode/dist/`

**Критерии приёмки**:

- ✅ `packages/metamode/vite.config.ts` существует с корректной Library Mode конфигурацией
- ✅ `packages/metamode/package.json` имеет `build` скрипт
- ✅ `packages/metamode/package.json` указывает `main`, `module`, `types` на `dist/`
- ✅ `.gitignore` исключает `packages/metamode/dist/`
- ✅ Пакет готов к сборке командой `cd packages/metamode && npm install && npm run build`

**Метки**: `package`, `github-packages`, `vite`, `build`, `critical`

---

### TASK 89: CI/CD для публикации в GitHub Packages ✅

**Заголовок**: `@netkeep80/metamode Phase 2 — CI/CD workflow for GitHub Packages publishing`

**Приоритет**: Высокий

**Статус**: ✅ Завершено

**Описание**:
Создан GitHub Actions workflow для автоматической публикации пакета `@netkeep80/metamode` в GitHub Packages при создании тега `metamode-v*` или GitHub Release. Публикация идёт через `https://npm.pkg.github.com`, а не через npmjs.com.

**Задачи**:

- [x] Создать `.github/workflows/publish-metamode.yml`
- [x] Настроить триггер на `push` тега `metamode-v*` или создание Release
- [x] Добавить шаги: install, build, test, publish
- [x] Настроить `registry-url: https://npm.pkg.github.com` вместо npmjs
- [x] Использовать `GITHUB_TOKEN` (не NPM_TOKEN) для аутентификации

**Созданные файлы**:

- `.github/workflows/publish-metamode.yml` — workflow для публикации `@netkeep80/metamode` в GitHub Packages

**Особенности реализации**:

- **Триггеры**: `push` тега `metamode-v*` (e.g. `metamode-v0.1.0`) и создание GitHub Release
- **Реестр**: `https://npm.pkg.github.com` (GitHub Packages, не npmjs.com)
- **Аутентификация**: `GITHUB_TOKEN` с правами `packages: write` (автоматически доступен в Actions)
- **Джобы**:
  - `build-and-test` — запускает тесты основного проекта, собирает пакет, верифицирует `dist/` артефакты
  - `publish` — проверяет соответствие версии тегу, публикует в GitHub Packages
- **Версионная защита**: workflow проверяет, что версия в `packages/metamode/package.json` совпадает с версией в теге. При несоответствии — публикация прерывается с ошибкой
- **Публичный доступ**: `--access public` для публичной видимости пакета
- **Concurrency**: одновременно может быть только одна активная публикация

**Необходимые секреты**:

| Секрет | Описание |
|--------|----------|
| `GITHUB_TOKEN` | Автоматически предоставляется GitHub Actions. Дополнительных секретов не требуется. |

**Процесс публикации**:

```bash
# 1. Обновить версию в packages/metamode/package.json
# 2. Закоммитить изменения
git commit -am "chore: bump @netkeep80/metamode to 0.2.0"
git push origin main

# 3. Создать и запушить тег
git tag metamode-v0.2.0
git push origin metamode-v0.2.0

# Workflow автоматически:
# - Запустит тесты основного проекта
# - Соберёт пакет (dist/)
# - Верифицирует соответствие версий
# - Опубликует @netkeep80/metamode@0.2.0 в GitHub Packages
```

**Критерии приёмки**:

- ✅ `.github/workflows/publish-metamode.yml` создан с корректной конфигурацией
- ✅ Триггер на тег `metamode-v*` и GitHub Release
- ✅ Реестр: `https://npm.pkg.github.com`
- ✅ Шаги: install, build main project tests, build package, verify dist/, publish
- ✅ Верификация версии: package.json vs tag
- ✅ `GITHUB_TOKEN` используется для аутентификации

**Метки**: `ci`, `github-packages`, `github-actions`, `automation`, `high`

---

### TASK 90: Тестирование пакета ✅

**Заголовок**: `@netkeep80/metamode Phase 3 — Тесты пакета с @vue/test-utils`

**Приоритет**: Высокий

**Статус**: ✅ Завершено

**Описание**:
Добавлены собственные тесты (Vitest + @vue/test-utils) в пакет `packages/metamode`, чтобы пакет мог тестироваться независимо от основного проекта.

**Задачи**:

- [x] Добавить devDependencies: vitest, @vue/test-utils, jsdom
- [x] Создать `packages/metamode/vitest.config.ts`
- [x] Написать тесты для `provideMetaMode`, `useMetaMode`
- [x] Написать тесты для утилит (типы, хелперы)
- [x] Добавить `test` скрипт в `package.json`

**Созданные файлы**:

- `packages/metamode/vitest.config.ts` — конфигурация Vitest с jsdom окружением
- `packages/metamode/src/types/__tests__/common.test.ts` — тесты `detectLanguage`, `DEFAULT_COMPONENT_REGISTRY`
- `packages/metamode/src/types/__tests__/metamode.test.ts` — тесты утилит хранилища, табов, ограничений, клавиатурных сокращений, диалогов
- `packages/metamode/src/components/__tests__/MetaModeProvider.test.ts` — тесты `provideMetaMode`, `useMetaMode`
- `packages/metamode/src/types/__tests__/issue-generator.test.ts` — тесты генерации задач, валидации черновиков

**Изменённые файлы**:

- `packages/metamode/package.json` — добавлены devDependencies (vitest, @vue/test-utils, jsdom) и скрипты `test`, `test:watch`, `test:coverage`
- `package.json` — добавлен скрипт `package:test` для запуска тестов пакета из корня
- `docs/phase-14.md` — обновлён статус TASK 90

**Результат тестов**:

- **130 тестов** в 4 файлах — все проходят ✅
- Покрытие: `provideMetaMode`, `useMetaMode`, `detectLanguage`, `loadMetaModeState`, `saveMetaModeState`, `isValidTab`, `getTabInfo`, `getAvailableTabs`, `clamp`, `constrainPosition`, `constrainSize`, `parseShortcut`, `matchesShortcut`, `createMessage`, `createSession`, `validateIssueDraft`, `createIssueDraft` и другие утилиты

**Критерии приёмки**:

- ✅ `vitest.config.ts` создан с jsdom окружением
- ✅ Тесты для `provideMetaMode`, `useMetaMode` написаны
- ✅ Тесты для утилит (типы, хелперы) написаны
- ✅ `test` скрипт добавлен в `package.json`
- ✅ Все тесты проходят: `npm run package:test`

**Метки**: `tests`, `vitest`, `vue`, `medium`

---

### TASK 91: Интеграционный тест ✅

**Заголовок**: `@netkeep80/metamode Phase 4 — Интеграционный тест использования пакета`

**Приоритет**: Средний

**Статус**: ✅ Завершено

**Описание**:
Создан `examples/basic-app/` с интеграционными тестами, которые демонстрируют и верифицируют использование собранного `dist/` пакета.

**Задачи**:

- [x] Создать `packages/metamode/examples/basic-app/` с минимальным Vue.js приложением
- [x] Импортировать `@netkeep80/metamode` из `dist/` (не из `src/`)
- [x] Добавить `test:integration` скрипт

**Созданные файлы**:

- `packages/metamode/examples/basic-app/integration.test.ts` — 35 интеграционных тестов, импортирующих из `dist/metamode.js`
- `packages/metamode/examples/basic-app/README.md` — документация интеграционного теста
- `packages/metamode/vitest.integration.config.ts` — конфигурация Vitest для интеграционных тестов

**Изменённые файлы**:

- `packages/metamode/package.json` — добавлен скрипт `test:integration`
- `packages/metamode/src/types/issue-generator.ts` — исправлен устаревший импорт `./god-mode` → `./metamode`

**Результат тестов**:

- **35 интеграционных тестов** — все проходят ✅
- Покрытие: все публичные экспорты из `dist/` — `provideMetaMode`, `useMetaMode`, `detectLanguage`, `isValidTab`, `getTabInfo`, `getAvailableTabs`, `clamp`, `constrainPosition`, `constrainSize`, `parseShortcut`, `matchesShortcut`, `loadMetaModeState`, `saveMetaModeState`, `createMessage`, `createSession`, `createIssueDraft`, `validateIssueDraft`, `getDefaultLabels` и константы

**Критерии приёмки**:

- ✅ `packages/metamode/examples/basic-app/integration.test.ts` создан
- ✅ Тест импортирует из `dist/metamode.js` (не из `src/`)
- ✅ `test:integration` скрипт добавлен в `package.json`
- ✅ Все 35 тестов проходят: `npm run test:integration` (требует предварительный `npm run build`)

**Метки**: `integration`, `examples`, `vue`, `medium`

---

### TASK 92: Подробная инструкция по выделению пакета в GitHub-репозиторий 📋

**Заголовок**: `@netkeep80/metamode Phase 5 — Инструкция по ручному выделению и подключению пакета`

**Приоритет**: Низкий

**Статус**: ✅ Завершено

**Описание**:
Создать подробную пошаговую инструкцию по ручному выделению пакета `@netkeep80/metamode` из `https://github.com/netkeep80/isocubic/tree/main/packages/metamode` в отдельный GitHub-репозиторий и повторному подключению его обратно в `https://github.com/netkeep80/isocubic` как внешней зависимости.

**Задачи**:

- [x] Создать файл `docs/metamode-extraction-guide.md` с полной пошаговой инструкцией
- [x] Описать шаги по созданию нового GitHub-репозитория `netkeep80/metamode`
- [x] Описать перенос файлов из `packages/metamode/` в новый репозиторий
- [x] Описать настройку GitHub Packages для публикации `@netkeep80/metamode`
- [x] Описать процесс первой публикации пакета
- [x] Описать подключение опубликованного пакета обратно в `isocubic` как внешней зависимости
- [x] Описать настройку `.npmrc` в `isocubic` для работы с GitHub Packages

**Созданные файлы**:

- `docs/metamode-extraction-guide.md` — подробная пошаговая инструкция по выделению пакета (7 шагов, устранение ошибок)

**Метки**: `documentation`, `github-packages`, `release`, `low`

---

## Порядок выполнения TASK

1. **TASK 88**: Vite Library Mode конфигурация ✅
2. **TASK 89**: CI/CD для GitHub Packages публикации ✅
3. **TASK 90**: Тестирование пакета ✅
4. **TASK 91**: Интеграционный тест ✅
5. **TASK 92**: Инструкция по выделению пакета ✅

---

## Оценка объёма работ

| TASK | Сложность | Приоритет | Статус |
|------|-----------|-----------|--------|
| 88. Vite Library Mode (Phase 1) | Средняя | Критический | ✅ Завершена |
| 89. CI/CD для GitHub Packages (Phase 2) | Средняя | Высокий | ✅ Завершено |
| 90. Тестирование пакета (Phase 3) | Средняя | Высокий | ✅ Завершено |
| 91. Интеграционный тест (Phase 4) | Средняя | Средний | ✅ Завершено |
| 92. Инструкция по выделению (Phase 5) | Низкая | Низкий | ✅ Завершено |

---

## Важные принципы

1. **Обратная совместимость** — `src/` API остаётся стабильным при переходе на `dist/`
2. **Peer dependencies** — Vue.js указан как peerDependency, не bundled
3. **Tree-shaking** — Library Mode гарантирует ES Module с tree-shaking поддержкой
4. **TypeScript** — полные `.d.ts` типы в `dist/` для TypeScript пользователей
5. **GitHub Packages** — публикация через `https://npm.pkg.github.com`, не npmjs.com
6. **Без дополнительных токенов** — `GITHUB_TOKEN` предоставляется автоматически Actions

---

**Назад к [README](../README.md)**
