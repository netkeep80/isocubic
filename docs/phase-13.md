# Фаза 13: Выделение @isocubic/metamode в отдельный NPM-пакет

Данный документ описывает задачи по выделению пакета `@isocubic/metamode` из монорепозитория в полноценный самостоятельный NPM-пакет с собственной системой сборки, тестирования и публикации.

**Статус**: 🔄 В процессе

---

## Обзор

**Цель**: Выделить `packages/metamode` в полноценный NPM-пакет, готовый к публикации в npm registry, с собственной системой сборки, тестами и CI.

**Предпосылки**: TASK 79 (Phase 12) подготовил пакет к выделению:
- Чистый public API задокументирован
- Минимум зависимостей (только Vue 3.5.0+ как peer dependency)
- Полная документация (README.md, examples)
- Корректный package.json с metadata для NPM

**Текущая проблема**: Пакет использует TypeScript исходники напрямую (`"main": "./src/index.ts"`) без компиляции — невозможно опубликовать в npm registry и использовать в проектах без TypeScript.

**Решение**: Создать полноценную систему сборки и подготовить пакет к публикации:

1. Добавить систему сборки (tsup) для компиляции TypeScript → JavaScript + .d.ts
2. Добавить локальные тесты для пакета
3. Настроить CI для пакета
4. Удалить deprecated backward-compatible алиасы GodMode
5. Обновить основной проект для использования собранного пакета

---

## Планируемые TASK

### TASK 82: Система сборки для @isocubic/metamode ✅

**Заголовок**: `Добавить систему сборки для пакета @isocubic/metamode`

**Приоритет**: Критический

**Статус**: ✅ Завершена

**Описание**:
Добавить конфигурацию `tsup` для сборки пакета в форматы ESM и CJS с генерацией TypeScript деклараций.

**Задачи**:

- [x] Добавить `tsup` как devDependency
- [x] Создать `tsup.config.ts` с конфигурацией сборки
- [x] Обновить `package.json`: поля main, module, types, exports, scripts
- [x] Добавить npm скрипт `build` для сборки пакета
- [x] Добавить `.gitignore` для игнорирования `dist/`
- [x] Проверить сборку: `npm run build` в packages/metamode

**Результаты**:

- Добавлен `tsup` для сборки TypeScript в ESM + CJS форматы
- Создан `tsup.config.ts` с конфигурацией: ESM + CJS, declaration files, source maps
- Обновлён `package.json` с корректными путями к dist/ для main, module, types
- Добавлен скрипт `build` в package.json
- Добавлен `.gitignore` для dist/ директории
- Сборка проходит успешно

**Критерии приёмки**:

- ✅ `npm run build` в packages/metamode создаёт dist/ с JS и .d.ts файлами
- ✅ Все тесты основного проекта проходят
- ✅ CI/CD сборка основного проекта проходит

**Метки**: `metamode`, `package`, `build`, `critical`

---

### TASK 83: Тесты для @isocubic/metamode

**Заголовок**: `Добавить тесты для пакета @isocubic/metamode`

**Приоритет**: Высокий

**Статус**: 📋 Запланирована

**Описание**:
Добавить тесты для пакета `@isocubic/metamode` с использованием Vitest, покрывающие public API.

**Задачи**:

- [ ] Добавить vitest как devDependency
- [ ] Создать тесты для types/common.ts (detectLanguage, ComponentMeta)
- [ ] Создать тесты для types/metamode.ts (tab utils, window utils, shortcuts, persistence)
- [ ] Создать тесты для types/issue-generator.ts (validation, creation, templates)
- [ ] Создать тесты для components/MetaModeProvider.ts (provide/inject)
- [ ] Добавить npm скрипт `test` для запуска тестов

**Критерии приёмки**:

- [ ] Покрытие public API > 80%
- [ ] Все тесты проходят
- [ ] CI/CD сборка проходит

**Метки**: `metamode`, `package`, `testing`, `high`

---

### TASK 84: Удаление deprecated GodMode алиасов

**Заголовок**: `Удалить deprecated GodMode backward-compatible алиасы`

**Приоритет**: Средний

**Статус**: 📋 Запланирована

**Описание**:
Удалить все deprecated алиасы обратной совместимости с GodMode:

- Type aliases: GodModeTab, GodModeTabInfo, GodModeConfig, GodModeWindowState, GodModeContextValue
- Constants: GOD_MODE_TABS, DEFAULT_GOD_MODE_CONFIG
- Functions: provideGodMode, useGodMode, GOD_MODE_KEY
- Functions: loadGodModeState, saveGodModeState

**Задачи**:

- [ ] Удалить deprecated exports из index.ts
- [ ] Удалить deprecated aliases из MetaModeProvider.ts
- [ ] Обновить основной проект (если есть использования)
- [ ] Обновить тесты

**Критерии приёмки**:

- [ ] Нет упоминаний GodMode в пакете
- [ ] Все тесты проходят
- [ ] CI/CD сборка проходит

**Метки**: `metamode`, `package`, `cleanup`, `medium`

---

### TASK 85: CI/CD для @isocubic/metamode

**Заголовок**: `Настроить CI для сборки и тестирования пакета @isocubic/metamode`

**Приоритет**: Средний

**Статус**: 📋 Запланирована

**Описание**:
Добавить CI workflow для сборки и тестирования пакета при изменениях в `packages/metamode/`.

**Задачи**:

- [ ] Добавить шаг сборки пакета в существующий CI workflow
- [ ] Добавить шаг тестирования пакета
- [ ] Добавить проверку типов для пакета

**Критерии приёмки**:

- [ ] CI автоматически собирает и тестирует пакет
- [ ] Ошибки в пакете блокируют merge

**Метки**: `metamode`, `package`, `ci`, `medium`

---

### TASK 86: Публикация @isocubic/metamode в npm

**Заголовок**: `Подготовить и опубликовать @isocubic/metamode в npm registry`

**Приоритет**: Низкий

**Статус**: 📋 Запланирована

**Описание**:
Подготовить финальную конфигурацию и опубликовать пакет в npm registry.

**Задачи**:

- [ ] Проверить package.json metadata (description, keywords, repository, homepage)
- [ ] Добавить CHANGELOG.md
- [ ] Добавить npm prepublish hook для автоматической сборки
- [ ] Протестировать публикацию с `npm pack`
- [ ] Документировать процесс публикации

**Критерии приёмки**:

- [ ] `npm pack` создаёт корректный tarball
- [ ] Пакет можно установить из npm
- [ ] Документация полная и актуальная

**Метки**: `metamode`, `package`, `publishing`, `low`

---

## Порядок выполнения TASK

Рекомендуемый порядок для обеспечения работоспособности после каждой задачи:

1. **TASK 82**: Система сборки (без неё пакет нельзя опубликовать)
2. **TASK 83**: Тесты (гарантируют корректность API)
3. **TASK 84**: Удаление deprecated алиасов (очистка перед публикацией)
4. **TASK 85**: CI/CD для пакета (автоматизация)
5. **TASK 86**: Публикация в npm (финальный шаг)

---

## Оценка объёма работ

| TASK                             | Сложность | Приоритет   | Статус       |
| -------------------------------- | --------- | ----------- | ------------ |
| 82. Система сборки               | Средняя   | Критический | ✅ Завершена |
| 83. Тесты пакета                | Средняя   | Высокий     | 📋 Запланирована |
| 84. Удаление deprecated алиасов | Низкая    | Средний     | 📋 Запланирована |
| 85. CI/CD для пакета            | Средняя   | Средний     | 📋 Запланирована |
| 86. Публикация в npm            | Низкая    | Низкий      | 📋 Запланирована |

---

## Важные принципы

1. **Каждая задача должна оставлять проект в рабочем состоянии**
   - CI/CD проходит после каждого TASK
   - Все тесты зелёные
   - Приложение запускается и работает

2. **Минимальное влияние на основной проект**
   - Пакет собирается независимо
   - Основной проект продолжает использовать исходники напрямую
   - После публикации можно перейти на npm-версию

3. **Стандартные практики NPM**
   - Semantic versioning
   - Корректные peer dependencies
   - Поддержка ESM и CJS
   - TypeScript declarations включены

---

**Назад к [README](../README.md)**
