{
  "$schema": "../../metamode.schema.json",
  "name": "workflows",
  "description": "GitHub Actions CI/CD workflows",
  "files": {
    "ci.yml": {
      "description": "Continuous integration: lint, typecheck, test, build across platforms"
    },
    "deploy.yml": {
      "description": "Automatic deployment to GitHub Pages on push to main"
    }
  }
}
